
generate_bin_name(BIN)

koll_add_test(
    TEST_NAME
        ${BIN}
    PRIVATE
        remove.cc
        function.cc
        bind.cc
        count/count.cc
        count/count_if.cc
        count/all_of.cc
        count/any_of.cc
        count/none_of.cc
        find/find.cc
        find/find_if.cc
        find/find_if_not.cc
        find/find_first_of.cc
        find/find_end.cc
        find/search.cc
        find/search_n.cc
        find/adjacent_find.cc
        sort/sort.cc
        sort/stable_sort.cc
        sort/max_element.cc
        sort/min_element.cc
        sort/shuffle.cc
        sort/upper_bound.cc
        sort/lower_bound.cc
        sort/partial_sort.cc
        ../main.cc
    DEPENDENCIES
        gtest
        koll
)