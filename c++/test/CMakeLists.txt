cmake_minimum_required(VERSION 3.10)
project(myproj CXX)
if(NOT DEFINED RELEASE_F)
    message("debug version\n")
    add_definitions(-g)
endif()
add_definitions(-std=c++11)
aux_source_directory(. SRCS)
add_executable(a.out ${SRCS})
